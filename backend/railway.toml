[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn core.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --log-file -"
healthcheckPath = "/api/aboutme/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
